# Environnement Python local (a recreer avec pip install -r requirements.txt)
venv/
__pycache__/
*.pyc

# Secrets : mot de passe MySQL, cle secrete Flask -> ne JAMAIS pousser sur GitHub
.env

# Dump complet de la base (structure + donnees reelles, dont la table `user`
# avec les hash de mots de passe) -> local uniquement, jamais sur GitHub.
# Regenerer avec mysqldump (voir services/sauvegarde.py / DEPLOIEMENT_O2SWITCH.md).
Create_yvallet_base_WithData.sql

# Fichiers generes a l'execution
logs/
export_csv/
sauvegarde-*.sql

# Genere par cPanel "Setup Python App" (Passenger) sur le serveur o2switch :
# specifique a la machine (chemins, config locale) -> ne pas versionner.
Passengerfile.json

# Divers
.DS_Store
Thumbs.db
